`create_ds` creates a dataset in alpaca format (instruction, input, output)
`finetune.py` finetune the krikri model (requires gpu)
`bert.py` finetune greek bert model 
`bert_eval.py` evaluate finetuned bert on example sentences
`bert_plot.py` plot the pca of greek and cypriot embeddings
`bert_plot2.py` plot the pca of the finetunned cypriot embeddings